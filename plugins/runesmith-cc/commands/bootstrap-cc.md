---
description: Build the Claude Code monorepo head at the workspace root, add comms folder, optionally create or clone repos with PAT-based clone.
---

Invoke the `bootstrap-cc` skill in this plugin.

This command is a thin wrapper - the skill at `skills/bootstrap-cc/SKILL.md` does all the work and is also auto-invoked by description match. Use this slash form when you want to invoke explicitly.
