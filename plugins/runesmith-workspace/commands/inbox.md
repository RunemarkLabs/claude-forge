---
description: Process files in _INBOX/ - classify each, propose a destination, route on consent.
---

Invoke the `inbox` skill in this plugin.

This command is a thin wrapper - the skill at `skills/inbox/SKILL.md` does all the work and is also auto-invoked by description match. Use this slash form when you want to invoke explicitly.
