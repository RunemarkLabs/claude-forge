---
description: Browse and install plugins from the bootstrap catalog and optional sources.
---

Invoke the `install` skill in this plugin.

This command is a thin wrapper — the skill at `skills/install/SKILL.md` does all the work and is also auto-invoked by description match. Use this slash form when you want to invoke explicitly.
