---
description: Sync installed plugins with latest versions and update AIOPS documentation.
---

Invoke the `sync` skill in this plugin.

This command is a thin wrapper — the skill at `skills/sync/SKILL.md` does all the work and is also auto-invoked by description match. Use this slash form when you want to invoke explicitly.
