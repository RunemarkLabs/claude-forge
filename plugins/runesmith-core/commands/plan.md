---
description: Plan a change before executing — capture problem, decision, why-now, scope, trade-offs to a clean markdown file under plans/active/.
---

Invoke the `plan` skill in this plugin.

This command is a thin wrapper — the skill at `skills/plan/SKILL.md` does all the work and is also auto-invoked by description match. Use this slash form when you want to invoke explicitly.
