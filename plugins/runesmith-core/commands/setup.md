---
description: Configure credentials, API endpoints, and plugin sources.
---

Invoke the `setup` skill in this plugin.

This command is a thin wrapper — the skill at `skills/setup/SKILL.md` does all the work and is also auto-invoked by description match. Use this slash form when you want to invoke explicitly.
