---
description: Create Cowork plugins and install them into Claude Desktop.
---

Invoke the `plugin-builder` skill in this plugin.

This command is a thin wrapper - the skill at `skills/plugin-builder/SKILL.md` does all the work and is also auto-invoked by description match. Use this slash form when you want to invoke explicitly.
