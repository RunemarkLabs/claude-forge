---
description: Propagate architectural decisions and convention changes across all skills in installed plugins and bootstrap repos.
---

Invoke the `skill-updater` skill in this plugin.

This command is a thin wrapper - the skill at `skills/skill-updater/SKILL.md` does all the work and is also auto-invoked by description match. Use this slash form when you want to invoke explicitly.
