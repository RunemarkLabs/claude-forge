---
description: Show all available workspace skills, installed plugins, and what they do.
---

Invoke the `help` skill in this plugin.

This command is a thin wrapper — the skill at `skills/help/SKILL.md` does all the work and is also auto-invoked by description match. Use this slash form when you want to invoke explicitly.
