---
description: Triage open comms in the CC head — surface to-user items, draft replies, archive resolved pairs.
---

Invoke the `check-comms` skill in this plugin.

This command is a thin wrapper — the skill at `skills/check-comms/SKILL.md` does all the work and is also auto-invoked by description match. Use this slash form when you want to invoke explicitly.
