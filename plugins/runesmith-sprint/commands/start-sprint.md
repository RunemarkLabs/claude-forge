---
description: Hand the active (or specified) sprint to Claude Code via a session-init comm.
---

Invoke the `start-sprint` skill in this plugin.

This command is a thin wrapper — the skill at `skills/start-sprint/SKILL.md` does all the work and is also auto-invoked by description match. Use this slash form when you want to invoke explicitly.
