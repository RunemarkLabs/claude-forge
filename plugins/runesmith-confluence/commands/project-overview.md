---
description: Create a project overview page for Confluence — the landing page for a project space.
---

Invoke the `project-overview` skill in this plugin.

This command is a thin wrapper — the skill at `skills/project-overview/SKILL.md` does all the work and is also auto-invoked by description match. Use this slash form when you want to invoke explicitly.
